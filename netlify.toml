[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  node_bundler = "esbuild"
  included_files = ["public/headshots/**"]

[functions."___netlify-server-handler"]
  timeout = 60

[functions."api/generate"]
  timeout = 60
